# UiaPeek (optional vendor drop)

Place `UiaPeek.exe` from the [UiaPeek releases](https://github.com/g4-api/uia-peek/releases) in this folder when the machine cannot reach GitHub. The Electron app checks here before downloading into app userData.

File name must be `UiaPeek.exe` (Windows).
